'use client';

import React from 'react';
import CookieConsent from '../ui/CookieConsent';

export default function CookieConsentProvider() {
  // CookieConsent component handles its own state and logic
  return <CookieConsent />;
} 